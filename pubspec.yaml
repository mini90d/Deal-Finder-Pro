name: deal_finder_pro
description: AI-powered mobile app for finding deals with AR gamification.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  provider: ^6.0.5
  camera: ^0.10.3
  arcore_flutter_plugin: ^0.1.0
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
